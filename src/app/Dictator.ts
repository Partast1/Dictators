
export class Dictator {
  firstName: string;
  lastName: string;
  born: string;
  deceased: string;
  description: string;
  constructor(firstName, lastName, born, deceased, description) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.born = born;
    this.deceased = deceased;
    this.description = description;

  }
}
